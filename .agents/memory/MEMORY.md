- [Supabase Storage bucket creation blocked](supabase-bucket-rls.md) — anon and authed user JWTs cannot create buckets; only service_role can. Use service_role key or Supabase dashboard.
- [Admin cloud sync migrated to tables](admin-config-sync.md) — admin_config / user_profiles / payment_screenshots tables replace old user_metadata hack; anon RLS policies so admin (no Supabase session) can read/write.
- [File cross-device sync](file-cross-device.md) — files ≤50MB stored as base64 in DB content column; NO Supabase Storage used (removed). syncFromSupabase uses metadata-only select + lazy content batch fetch.
- [Sync architecture and silent failures](sync-architecture.md) — all write failures were silently swallowed; now log to console.error. uploadLocalDataToCloud upserts all local items when sync is broken. syncStats/syncLoading/syncError states added for UI feedback.
- [Admin premium requests cloud](admin-premium-requests.md) — premium_requests table in schema; approve/reject calls deleteTxScreenshotByEmail(email) to clean up payment_screenshots automatically.
